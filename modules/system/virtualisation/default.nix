{ ... }:

{
  # virtualization > virtualisation
  # Keeping the British English spelling for relevant files and folders
  # until NixOS supports the American English spelling, if ever.
  virtualisation.docker.enable = true;
}
